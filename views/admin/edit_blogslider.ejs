<%- include("navbar.ejs") %>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Slider</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<div class="container mt-4">
    <div class="form-box">
        <h5 class="text-primary text-center">Edit Slider</h5>
        <form action="/admin/update_blogslider/<%= slider.blog_id %>" method="POST" enctype="multipart/form-data">
            <div class="row g-3">
                <div class="col-md-6">
                    <input type="text" name="blog_title" class="form-control mb-3" value="<%= slider.blog_title %>" required>
                </div>
                <div class="col-md-6">
                    <input type="file" name="blog_image" class="form-control mb-3">
                    <small>Current Image:</small><br>
                    <img src="/admin_assets/<%= slider.blog_image || 'default.jpg' %>" class="img-thumbnail" width="80">
                </div>
                <div class="col-12">
                    <button type="submit" class="btn btn-primary w-100">Update Slider</button>
                </div>
            </div>
        </form>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<%- include("footer.ejs") %>
