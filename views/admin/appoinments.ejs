<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Appointment List</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        .table-container {
            background: #ffffff;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }
        .table th {
            background: #C71585;
            color: white;
            text-align: center;
        }
        .table tbody tr:hover {
            background: #f8f9fa;
        }
        .btn-danger {
            padding: 5px 10px;
            font-size: 14px;
        }
    </style>
</head>
<body>
<%-include("navbar.ejs")%>
<div class="container mt-5">
    <h2 class="text-center text-primary mb-4">Appointment List</h2>

    <div class="table-container">
        <table class="table table-bordered table-striped text-center">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Full Name</th>
                    <th>Mobile</th>
                    <th>Email</th>
                    <th>Appointment Date</th>
                    <th>Doctor Name</th>
                    <th>Message</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% if (appointments.length > 0) { %>
                    <% appointments.forEach(function(appointment) { %>
                    <tr>
                        <td><%= appointment.id %></td>
                        <td><%= appointment.name %></td>
                        <td><%= appointment.mobile %></td>
                        <td><%= appointment.email %></td>
                        <td><%= appointment.date %></td>
                        <td><%= appointment.doctor_name %></td>
                        <td><%= appointment.message %></td>
                        <td>
                            <a href="/admin/delete_appointment/<%= appointment.id %>" class="btn btn-danger btn-sm">Delete</a>
                        </td>
                    </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="8" class="text-center text-danger">No Appointments Found</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</div>
<%-include("footer.ejs")%>
</body>
</html>
