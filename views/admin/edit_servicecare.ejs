<%- include("navbar.ejs") %>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Service Care</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .container { max-width: 50%; margin-top: 30px; }
        .form-box {
            background: white; padding: 20px; border-radius: 8px;
            box-shadow: 2px 4px 10px rgba(0, 0, 0, 0.1);
        }
        .form-label { font-weight: 600; }
        .btn-custom { width: 100px; }
    </style>
</head>
<body>

<div class="container">
    <div class="form-box">
        <h5 class="text-primary text-center mb-3">Edit Service Care</h5>

        <form action="/admin/update_servicecare/<%= serviceCare.care_id %>" method="POST">
            <!-- Care Title -->
            <div class="mb-3">
                <label class="form-label">Care Title</label>
                <input type="text" name="care_title" class="form-control border-primary shadow-sm" 
                       value="<%= serviceCare.care_title %>" required>
            </div>

            <!-- Care Description -->
            <div class="mb-3">
                <label class="form-label">Care Description</label>
                <textarea name="care_description" class="form-control border-primary shadow-sm" rows="4" required>
                    <%= serviceCare.care_description %>
                </textarea>
            </div>

            <!-- Action Buttons -->
            <div class="d-flex justify-content-between">
                <a href="/admin/service_care" class="btn btn-secondary btn-custom">⬅ Back</a>
                <button type="submit" class="btn btn-success btn-custom">✅ Update</button>
            </div>
        </form>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>

<%- include("footer.ejs") %>
