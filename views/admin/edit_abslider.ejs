<%- include("navbar.ejs") %>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Slider</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f4f4f4; }
        .container { max-width: 50%; }
        .form-box {
            background: white; padding: 20px; border-radius: 8px;
            box-shadow: 2px 4px 15px rgba(0, 0, 0, 0.15);
        }
        .img-thumbnail { width: 100px; height: 100px; border-radius: 8px; }
    </style>
</head>
<body>

<div class="container mt-4">
    <div class="form-box">
        <h5 class="text-primary text-center">Edit Slider</h5>
        <form action="/admin/update_abslider/<%= aslider.aboutslider_id %>" method="POST" enctype="multipart/form-data">
            <div class="mb-3">
                <label class="form-label">Title</label>
                <input type="text" name="text1" class="form-control" value="<%= aslider.text1 %>" required>
            </div>
            <div class="mb-3">
                <label class="form-label">Current Image</label><br>
                <img src="/admin_assets/<%= aslider.img1 || 'default.jpg' %>" class="img-thumbnail">
            </div>
            <div class="mb-3">
                <label class="form-label">Upload New Image (Optional)</label>
                <input type="file" name="img1" class="form-control">
            </div>
            <button type="submit" class="btn btn-success w-100">Update Slider</button>
        </form>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<%- include("footer.ejs") %>
