<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bade Hospital</title>

    <!-- AOS for Animations -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- FontAwesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
<!-- Swiper CSS -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.css">

    <!-- External CSS -->
<style type="text/css">
    
        .img {
            height: 50px;
            width: 50px;
            border-radius: 50%;
            margin-right: 10px;
        }
        .dr {
            font-family: cursive;
            font-size: 20px;
            font-weight: bold;
        }

        /* Navbar Animation */
       /* Sticky Navbar */
       .first{
        margin-top: -70px;
         color: #900078; 
       }
       .first a{
         color:#900078 ;
       }
         .call-btn {
       
        background-color:   #9ACD32;
        color: white;
        font-weight: 600;
        padding: 8px 15px;
    }
    .call-btn:hover {
        background-color:   #9ACD32;
    }
  
.navbar {

    background-color: #FFC0CB !important;
/*    position: fixed;*/
    top: 0;
    width: 100%;
    z-index: 1000;
    transition: all 0.3s ease-in-out;
}
  .nav-link {
        font-size: 16px;
        font-weight: 400;
        padding: 10px 15px;
    }
    .nav-link:hover {
        color: #900078 !important;
    }

 .navbar-nav .nav-link.active {
    font-weight: bold;
    border-bottom: 2px solid #ff6600; /* Optional underline effect */
}
.navbar a {
    color: black !important; /* Change to your preferred text color */
    font-weight: bold;
    text-decoration: none;
}
/* Add padding so content doesn't hide behind navbar */
body {
    padding-top: 70px; /* Adjust based on navbar height */
}

/* Navbar shadow when scrolling */
.navbar.scrolled {
    background-color:   #FFC0CB !important; /* Darker shade */
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease-in-out;
}


        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Navbar Links Animation */
        .navbar-nav .nav-item {
            position: relative;
            margin: 0 10px;
        }

        .navbar-nav .nav-item .nav-link {
            font-size: 16px;
            font-weight: 500;
            transition: color 0.3s ease-in-out;
            position: relative;
        }

        .navbar-nav .nav-item .nav-link::after {
            content: '';
            position: absolute;
            left: 50%;
            bottom: -5px;
            width: 0;
            height: 3px;
            transition: all 0.4s ease-in-out;
            transform: translateX(-50%);
        }

        .navbar-nav .nav-item .nav-link:hover::after {
            width: 100%;
        }

        /* Dropdown Hover Effect */
        .dropdown-menu {
            background: #FFC0CB;
            border: none;
        }

        .dropdown-menu .dropdown-item {
            color: white;
            transition: all 0.3s ease-in-out;
        }

        .dropdown-menu .dropdown-item:hover {
            background: yellow;
            color: black;
        }

        /* Appointment Button */
        .app {
            background: black;
            color: white;
            border: none;
            padding: 8px 15px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 5px;
            transition: all 0.3s ease-in-out;
            margin-left: 15px;
        }

        .app:hover {
            background: grey;
            transform: scale(1.1);
            color: white;
        }

        /* Mobile View */
        @media (max-width: 991px) {
            .navbar-nav {
                text-align: center;
            }
            .app {
                width: 100%;
                margin: 10px 0;
            }
        }
 .hero-section {
    position: relative;
    width: 100%;
    height: 400px; /* Adjust height as needed */
    overflow: hidden;
    animation: fadeIn 1.5s ease-in-out;
}

@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(-20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Hero Image Styling */
.hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

/* Pink Overlay */
.hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(255, 192, 203, 0.2); /* Darker semi-transparent pink */
    display: flex;
    justify-content: center;
    align-items: center;
}

/* "About Us" Text Styling */
.hero-overlay h1 {
    color: white;
    font-size: 48px;
    font-weight: bold;
    text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.6);
    text-transform: uppercase;
}

/* Responsive Design */
@media (max-width: 768px) {
    .hero-section {
        height: 300px; /* Reduce height for mobile */
    }

    .hero-overlay h1 {
        font-size: 36px; /* Adjust text size */
    }
}

@media (max-width: 480px) {
    .hero-section {
        height: 250px; /* Further reduce height */
    }

    .hero-overlay h1 {
        font-size: 28px; /* Smaller text for small screens */
    }
}
/* Blog Section Animation */
.blog-card {
    border: none;
    border-radius: 10px;
    overflow: hidden;
    transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
}

.blog-card img {
    height: 200px;
    object-fit: cover;
    border-top-left-radius: 10px;
    border-top-right-radius: 10px;
    transition: transform 0.3s ease-in-out;
}

.blog-card:hover img {
    transform: scale(1.05);
}

.blog-card .card-body {
    padding: 20px;
    background: #f8f9fa;
    text-align: center;
}

.blog-card .card-title {
    font-size: 18px;
    font-weight: bold;
    color: #C71585;
}

.blog-card .card-text {
    font-size: 14px;
    color: #333;
}

.blog-card .btn {
    background-color: #C71585;
    border: none;
    color: #fff;
    transition: background 0.3s;
}

.blog-card .btn:hover {
    background-color: #a3126e;
}

/* Hover Effect */
.blog-card:hover {
    transform: translateY(-5px);
    box-shadow: 0px 10px 15px rgba(0, 0, 0, 0.1);
}

</style> 
</head>
<body>
<%-include("navbar.ejs")%>
    <div class="hero-section">
        <img src="/admin_assets/<%-bslider[0]['blog_image']%>" class="hero-image" alt="Blog">
        <div class="hero-overlay">
            <h1><%-bslider[0]['blog_title']%></h1>
        </div>
    </div>  
<section class="container my-5">
    <h2 class="text-center mb-4" style="color:#C71585;">Latest Health Articles</h2>
    <div class="row">
        <% blogCards.forEach(function(blog, index) { %>
            <div class="col-md-4" data-aos="fade-up" data-aos-delay="<%- index * 200 %>">
                <div class="card blog-card">
                    <img src="/admin_assets/<%- blog.card_image %>" class="card-img-top" alt="<%- blog.card_title %>">
                    <div class="card-body">
                        <h5 class="card-title"><%- blog.card_title %></h5>
                        <p class="card-text"><%- blog.card_description %></p>
                        <a href="#" class="btn btn-primary">Read More</a>
                    </div>
                </div>
            </div>
        <% }); %>
        
    </div>
</section>

<%-include("footer.ejs")%>

<script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
<script>
    AOS.init({
        duration: 1000, // Animation duration in milliseconds
        once: true, // Animation runs only once when scrolled into view
    });
</script>
</body>
<script>
document.addEventListener("DOMContentLoaded", function () {
    // Get the current page file name (without query parameters)
    let currentPage = window.location.pathname.split("/").pop();

    // If the URL ends with "/", assume it's "Navbar.html"
    if (currentPage === "" || currentPage === "index.html") {
        currentPage = "blog.html"; // Change this if your homepage file is different
    }

    // Select all navbar links
    let navLinks = document.querySelectorAll(".nav-link");

    // Remove 'active' class from all links
    navLinks.forEach(link => link.classList.remove("active"));

    // Loop through links and add "active" class only to the matching one
    navLinks.forEach(link => {
        if (link.getAttribute("href") === currentPage) {
            link.classList.add("active");
        }
    });
});
</script>
</html>
