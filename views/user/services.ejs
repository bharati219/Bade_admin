<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bade Hospital</title>

    <!-- AOS for Animations -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- FontAwesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
<!-- Swiper CSS -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.css">

    <!-- External CSS -->
<style type="text/css">
    
        .img {
            height: 50px;
            width: 50px;
            border-radius: 50%;
            margin-right: 10px;
        }
        .dr {
            font-family: cursive;
            font-size: 20px;
            font-weight: bold;
        }

        /* Navbar Animation */
       /* Sticky Navbar */
       .first{
        margin-top: -70px;
         color: #900078; 
       }
       .first a{
         color:#900078 ;
       }
         .call-btn {
       
        background-color:   #9ACD32;
        color: white;
        font-weight: 600;
        padding: 8px 15px;
    }
    .call-btn:hover {
        background-color:   #9ACD32;
    }
  
.navbar {

    background-color: #FFC0CB !important;
/*    position: fixed;*/
    top: 0;
    width: 100%;
    z-index: 1000;
    transition: all 0.3s ease-in-out;
}
  .nav-link {
        font-size: 16px;
        font-weight: 400;
        padding: 10px 15px;
    }
    .nav-link:hover {
        color: #900078 !important;
    }

     .navbar-nav .nav-link.active {
    font-weight: bold;
    border-bottom: 2px solid #ff6600; /* Optional underline effect */
}

.navbar a {
    color: black !important; /* Change to your preferred text color */
    font-weight: bold;
    text-decoration: none;
}
/* Add padding so content doesn't hide behind navbar */
body {
    padding-top: 70px; /* Adjust based on navbar height */
}

/* Navbar shadow when scrolling */
.navbar.scrolled {
    background-color:   #FFC0CB !important; /* Darker shade */
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease-in-out;
}


        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Navbar Links Animation */
        .navbar-nav .nav-item {
            position: relative;
            margin: 0 10px;
        }

        .navbar-nav .nav-item .nav-link {
            font-size: 16px;
            font-weight: 500;
            transition: color 0.3s ease-in-out;
            position: relative;
        }

        .navbar-nav .nav-item .nav-link::after {
            content: '';
            position: absolute;
            left: 50%;
            bottom: -5px;
            width: 0;
            height: 3px;
            transition: all 0.4s ease-in-out;
            transform: translateX(-50%);
        }

        .navbar-nav .nav-item .nav-link:hover::after {
            width: 100%;
        }

        /* Dropdown Hover Effect */
        .dropdown-menu {
            background: #FFC0CB;
            border: none;
        }

        .dropdown-menu .dropdown-item {
            color: white;
            transition: all 0.3s ease-in-out;
        }

        .dropdown-menu .dropdown-item:hover {
            background: yellow;
            color: black;
        }

        /* Appointment Button */
        .app {
            background: black;
            color: white;
            border: none;
            padding: 8px 15px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 5px;
            transition: all 0.3s ease-in-out;
            margin-left: 15px;
        }

        .app:hover {
            background: grey;
            transform: scale(1.1);
            color: white;
        }

        /* Mobile View */
        @media (max-width: 991px) {
            .navbar-nav {
                text-align: center;
            }
            .app {
                width: 100%;
                margin: 10px 0;
            }
        }
 .hero-section {
    position: relative;
    width: 100%;
    height: 400px; /* Adjust height as needed */
    overflow: hidden;
    animation: fadeIn 1.5s ease-in-out;
}

@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(-20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Hero Image Styling */
.hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

/* Pink Overlay */
.hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(255, 192, 203, 0.2); /* Darker semi-transparent pink */
    display: flex;
    justify-content: center;
    align-items: center;
}

/* "About Us" Text Styling */
.hero-overlay h1 {
    color: white;
    font-size: 48px;
    font-weight: bold;
    text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.6);
    text-transform: uppercase;
}

/* Responsive Design */
@media (max-width: 768px) {
    .hero-section {
        height: 300px; /* Reduce height for mobile */
    }

    .hero-overlay h1 {
        font-size: 36px; /* Adjust text size */
    }
}

@media (max-width: 480px) {
    .hero-section {
        height: 250px; /* Further reduce height */
    }

    .hero-overlay h1 {
        font-size: 28px; /* Smaller text for small screens */
    }
}

       

/* Card Styling */
.card {
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    border: none;
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    border-radius: 10px;
    overflow: hidden;
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 0.8s ease-in-out forwards;
}

/* Hover Effect */
.card:hover {
    transform: scale(1.05);
    box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2);
}

/* Card Fade-In Animation */
@keyframes fadeInUp {
    0% {
        opacity: 0;
        transform: translateY(30px);
    }
    100% {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Responsive Grid */
@media (max-width: 768px) {
    .card {
        animation: fadeInUp 1s ease-in-out forwards;
    }
}
/* Section Title */
.section-title {
    text-align: center;
    color: #333;
    font-size: 2rem;
    margin-bottom: 30px;
}

/* Service Cards */
.service-card {
    position: relative;
    width: 100%;
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.service-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: opacity 0.3s ease;
}

/* Overlay */
.service-overlay {
    position: absolute;
    bottom: 0;
    width: 100%;
    background: rgba(255, 255, 255, 0.8);
    text-align: center;
    font-size: 1rem;
    font-weight: bold;
    padding: 10px;
    border-bottom-left-radius: 20px;
    border-bottom-right-radius: 20px;
}

/* Icon Inside Card */
.service-icon {
    width: 30px;
    height: 30px;
    margin-right: 10px;
    vertical-align: middle;
}

/* Hover Effect */
.service-card:hover {
    transform: translateY(-5px);
    box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2);
}

.service-card:hover img {
    opacity: 0.9;
}

/* Responsive Grid */
@media (max-width: 992px) {
    .service-card {
        margin-bottom: 20px;
    }
}

@media (max-width: 768px) {
    .col-sm-6 {
        flex: 0 0 50%;
        max-width: 50%;
    }
}

@media (max-width: 576px) {
    .col-sm-6 {
        flex: 0 0 100%;
        max-width: 100%;
    }
}

</style> 
</head>
<body>
    <%-include("navbar.ejs")%>
    <div class="hero-section mt-0">
    <img src="/admin_assets/<%-servslider[0]['img1']%>" class="hero-image" alt="Our Services">
    <div class="hero-overlay">
        <h1><%- servslider[0]['text1'] %></h1>
    </div>
</div> 
<br><br>
<section id="services" class="container my-5">
    <h2 style="color:#C71585; text-align: center;">Expert Care For Women & Children</h2>
    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-4">
        <% serviceCareList.forEach(function(service) { %>
            <div class="col-md-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title"><%- service.care_title %></h5>
                        <p class="card-text"><%- service.care_description %></p>
                    </div>
                </div>
            </div>
        <% }); %>
    </div>
</section>
<section id="services" class="container my-5">
    <h2 class="section-title">Our Specialized Services</h2>
    <div class="row">
        <% specialServiceList.forEach(function(service) { %>
            <div class="col-lg-3 col-md-4 col-sm-6">
                <div class="service-card">
                    <img src="/admin_assets/<%- service.special_image %>" alt="<%- service.special_title %>">
                    <div class="service-overlay"><%- service.special_title %></div>
                </div>
            </div>
        <% }); %>
    </div>
</section>



<!-- Ambulance Services Section -->
<section id="ambulance-services" class="container my-5">
    <h2 style="color:#C71585; text-align: center;">Ambulance Services</h2>
    <div class="row align-items-center"> 
        <!-- Ambulance Image with Animation -->
         <div class="col-md-6" data-aos="fade-left" data-aos-duration="1000">
            <img src="public/c2.jpg" alt="Ambulance Services" class="img-fluid rounded shadow-lg">
        </div> 

        <!-- Ambulance Service Information with Animation -->
         <div class="col-md-6" data-aos="fade-right" data-aos-duration="1000">
            <h3 class="mb-3">Emergency Ambulance Services</h3>
            <p>At Bade Hospital, we provide 24/7 emergency ambulance services to ensure fast and safe transportation for patients in critical conditions. Our fleet of fully equipped ambulances is staffed with experienced paramedics who are trained to provide immediate medical care during transit.</p>
            
            <h4>Our Ambulance Features:</h4>
            <ul>
                <li>24/7 availability for emergency and non-emergency cases</li>
                <li>Equipped with advanced life-saving equipment</li>
                <li>Experienced paramedics and medical staff on board</li>
                <li>Safe and timely transportation to Bade Hospital</li>
                <li>Comfortable and hygienic vehicles for patient care</li>
            </ul>

            <h4>How to Request an Ambulance:</h4>
            <p>Simply call our emergency number <strong>0241-2424929</strong>, and our team will dispatch an ambulance to your location immediately. Our team is trained to handle critical situations with professionalism and urgency.</p>

            <p><strong>For immediate assistance, contact us anytime!</strong></p>
        </div>
    </div>
</section> 
<%-include("footer.ejs")%>


<script src="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.js"></script>
<script>
  AOS.init();
</script>
</body>
<script>
document.addEventListener("DOMContentLoaded", function () {
    // Get the current page file name (without query parameters)
    let currentPage = window.location.pathname.split("/").pop();

    // If the URL ends with "/", assume it's "Navbar.html"
    if (currentPage === "" || currentPage === "index.html") {
        currentPage = "services.html"; // Change this if your homepage file is different
    }

    // Select all navbar links
    let navLinks = document.querySelectorAll(".nav-link");

    // Remove 'active' class from all links
    navLinks.forEach(link => link.classList.remove("active"));

    // Loop through links and add "active" class only to the matching one
    navLinks.forEach(link => {
        if (link.getAttribute("href") === currentPage) {
            link.classList.add("active");
        }
    });
});
</script>
</html>