<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bade Hospital</title>

    <!-- AOS for Animations -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- FontAwesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
<!-- Swiper CSS -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.css">

    <!-- External CSS -->
<style type="text/css">
    
        .img {
            height: 50px;
            width: 50px;
            border-radius: 50%;
            margin-right: 10px;
        }
        .dr {
            font-family: cursive;
            font-size: 20px;
            font-weight: bold;
        }

        /* Navbar Animation */
       /* Sticky Navbar */
       .first{
        margin-top: -70px;
         color: #900078; 
       }
       .first a{
         color:#900078 ;
       }
         .call-btn {
       
        background-color:   #9ACD32;
        color: white;
        font-weight: 600;
        padding: 8px 15px;
    }
    .call-btn:hover {
        background-color:   #9ACD32;
    }
  
.navbar {

    background-color: #FFC0CB !important;
/*    position: fixed;*/
    top: 0;
    width: 100%;
    z-index: 1000;
    transition: all 0.3s ease-in-out;
}
  .nav-link {
        font-size: 16px;
        font-weight: 400;
        padding: 10px 15px;
    }
    .nav-link:hover {
        color: #900078 !important;
    }
     .navbar-nav .nav-link.active {
    font-weight: bold;
    border-bottom: 2px solid #ff6600; /* Optional underline effect */
}

.navbar a {
    color: black !important; /* Change to your preferred text color */
    font-weight: bold;
    text-decoration: none;
}
/* Add padding so content doesn't hide behind navbar */
body {
    padding-top: 70px; /* Adjust based on navbar height */
}

/* Navbar shadow when scrolling */
.navbar.scrolled {
    background-color:   #FFC0CB !important; /* Darker shade */
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease-in-out;
}


        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Navbar Links Animation */
        .navbar-nav .nav-item {
            position: relative;
            margin: 0 10px;
        }

        .navbar-nav .nav-item .nav-link {
            font-size: 16px;
            font-weight: 500;
            transition: color 0.3s ease-in-out;
            position: relative;
        }

        .navbar-nav .nav-item .nav-link::after {
            content: '';
            position: absolute;
            left: 50%;
            bottom: -5px;
            width: 0;
            height: 3px;
            transition: all 0.4s ease-in-out;
            transform: translateX(-50%);
        }

        .navbar-nav .nav-item .nav-link:hover::after {
            width: 100%;
        }

        

        /* Dropdown Hover Effect */
        .dropdown-menu {
            background: #FFC0CB;
            border: none;
        }

        .dropdown-menu .dropdown-item {
            color: white;
            transition: all 0.3s ease-in-out;
        }

        .dropdown-menu .dropdown-item:hover {
            background: yellow;
            color: black;
        }

        /* Appointment Button */
        .app {
            background: black;
            color: white;
            border: none;
            padding: 8px 15px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 5px;
            transition: all 0.3s ease-in-out;
            margin-left: 15px;
        }

        .app:hover {
            background: grey;
            transform: scale(1.1);
            color: white;
        }

        /* Mobile View */
        @media (max-width: 991px) {
            .navbar-nav {
                text-align: center;
            }
            .app {
                width: 100%;
                margin: 10px 0;
            }
        }
 .hero-section {
    position: relative;
    width: 100%;
    height: 400px; /* Adjust height as needed */
    overflow: hidden;
    animation: fadeIn 1.5s ease-in-out;
}

@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(-20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Hero Image Styling */
.hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

/* Pink Overlay */
.hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(255, 192, 203, 0.2); /* Darker semi-transparent pink */
    display: flex;
    justify-content: center;
    align-items: center;
}

/* "About Us" Text Styling */
.hero-overlay h1 {
    color: white;
    font-size: 48px;
    font-weight: bold;
    text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.6);
    text-transform: uppercase;
}

/* Responsive Design */
@media (max-width: 768px) {
    .hero-section {
        height: 300px; /* Reduce height for mobile */
    }

    .hero-overlay h1 {
        font-size: 36px; /* Adjust text size */
    }
}

@media (max-width: 480px) {
    .hero-section {
        height: 250px; /* Further reduce height */
    }

    .hero-overlay h1 {
        font-size: 28px; /* Smaller text for small screens */
    }
}

.contact-section {
    padding: 50px 0;
    background-color: #f8f9fa;
}

/* Header Styles */
h2 {
    font-size: 2.5rem;
    font-weight: bold;
    color: #C71585;
}

p.text-center {
    font-size: 1.1rem;
    color: #333;
}

/* Column Layout */
.row {
    margin-top: 40px;
}

/* Left Column (Contact Information) */
.col-md-6 h4 {
    font-size: 1.8rem;
    font-weight: bold;
    color: #333;
    margin-bottom: 15px;
}

.col-md-6 p {
    font-size: 1rem;
    color: #555;
}

/* Form Styles */
.contact-form {
    background-color: #fff;
    padding: 30px;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.contact-form .form-label {
    font-weight: 600;
    color: #333;
}

.contact-form .form-control {
    border-radius: 5px;
    border: 1px solid #ccc;
    padding: 10px;
    font-size: 1rem;
    margin-bottom: 20px;
    transition: border 0.3s;
}

.contact-form .form-control:focus {
    border-color: #C71585;
    box-shadow: 0 0 5px rgba(199, 21, 133, 0.5);
}

.contact-form button {
    padding: 12px;
    font-size: 1.2rem;
    color: #fff;
    background-color: #C71585;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s;
}

.contact-form button:hover {
    background-color: #a31470;
}

/* Map Section */
.map-container {
    margin-top: 40px;
}

.map-container iframe {
    border-radius: 8px;
}

@media (max-width: 768px) {
    .contact-section {
        padding: 30px 15px;
    }

    .row {
        flex-direction: column;
    }

    .col-md-6 {
        margin-bottom: 30px;
    }

    .contact-form {
        padding: 20px;
    }

    .contact-form button {
        font-size: 1rem;
    }
}
.footer {
    background-color: #222;
    color: #fff;
    text-align: center;
    padding: 30px 15px;
}

.footer .container {
    max-width: 900px;
    margin: auto;
}

.footer-top h3 {
    font-size: 24px;
    margin-bottom: 10px;
}

.footer-links {
    margin: 20px 0;
}

.footer-links a {
    color: #fff;
    margin: 0 10px;
    text-decoration: none;
    font-size: 16px;
    transition: 0.3s;
}

.footer-links a:hover {
    text-decoration: underline;
    color: #ff6600;
}

.footer-social {
    margin: 15px 0;
}

.footer-social a {
    color: #fff;
    font-size: 20px;
    margin: 0 10px;
    transition: 0.3s;
}

.footer-social a:hover {
    color: #ff6600;
    transform: scale(1.2);
}

.footer-bottom {
    margin-top: 20px;
    font-size: 14px;
    opacity: 0.8;
}

/* Responsive Design */
@media (max-width: 600px) {
    .footer-links {
        display: flex;
        flex-direction: column;
        gap: 10px;
    }

    .footer-social {
        display: flex;
        justify-content: center;
        gap: 15px;
    }

    .footer-links a, .footer-social a {
        font-size: 18px;
    }
}

</style> 
</head>
<body>
<%-include("navbar.ejs")%>
<div class="hero-section">
    <img src="public/c2.jpg" class="hero-image" alt="About Us">
    <div class="hero-overlay">
        <h1>Contact</h1>
    </div>
</div>
    <section class="contact-section">
        <div class="container">
            <h2 class="text-center"style="color:#C71585; text-align: center;">Contact Us</h2>
            <p class="text-center">We’re here to help! Feel free to reach out to us.</p>
            <div class="row">
                <div class="col-md-6">
                    <h4>Get in Touch</h4>
                    <p><strong>Address:</strong> Opposite Hotel Oberoi, Near Swami Samarth Temple, Savedi Road, Ahmednagar, Maharashtra, 414001. </p>
                    <p><strong>Phone:</strong>+91 241 242 4929</p>
                    <p><strong>Email:</strong>dr.badehospital@gmail.com</p>
                    <p><strong>Working Hours:</strong>The hospital operates 24 hours a day, seven days a week, ensuring round-the-clock medical assistance</p>
                </div>
                <div class="col-md-6">
                    <form class="contact-form" action="/save_contact" method="post" onsubmit="return validateForm()">
                        <div class="mb-3">
                            <label for="name" class="form-label">Your Name</label>
                            <input type="text" class="form-control" id="name" name="name" placeholder="Enter your name" required>
                        </div>
                        <div class="mb-3">
                            <label for="email" class="form-label">Your Email</label>
                            <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email" required>
                        </div>
                        <div class="mb-3">
                            <label for="message" class="form-label">Your Message</label>
                            <textarea class="form-control" id="message" name="message" rows="4" placeholder="Write your message here" required></textarea>
                        </div>
                        <button type="submit" class="btn w-100" style="background-color: #C71585">Send Message</button>
                    </form>
                    
                    <script>
                        function validateForm() {
                            let name = document.getElementById("name").value.trim();
                            let email = document.getElementById("email").value.trim();
                            let message = document.getElementById("message").value.trim();
                    
                            if (!name || !email || !message) {
                                alert("All fields are required.");
                                return false; // Prevent form submission
                            }
                            return true; // Allow form submission
                        }
                    </script>
                    
                </div>
            </div>
            
            <div class="map-container mt-5">
                <h4 class="text-center">Find Us on Google Maps</h4>
                <iframe src="https://www.google.com/maps/embed?...(Your Google Map Link Here)..." width="100%" height="400" style="border:0;" allowfullscreen></iframe>
            </div>
        </div>
    </section>
<%-include("footer.ejs")%>


</body>
<script>
document.addEventListener("DOMContentLoaded", function () {
    // Get the current page file name (without query parameters)
    let currentPage = window.location.pathname.split("/").pop();

    // If the URL ends with "/", assume it's "Navbar.html"
    if (currentPage === "" || currentPage === "index.html") {
        currentPage = "contact.html"; // Change this if your homepage file is different
    }

    // Select all navbar links
    let navLinks = document.querySelectorAll(".nav-link");

    // Remove 'active' class from all links
    navLinks.forEach(link => link.classList.remove("active"));

    // Loop through links and add "active" class only to the matching one
    navLinks.forEach(link => {
        if (link.getAttribute("href") === currentPage) {
            link.classList.add("active");
        }
    });
});
</script>
</html>
